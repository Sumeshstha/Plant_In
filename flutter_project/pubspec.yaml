name: plant_in
description: A comprehensive plant care companion, community, and garden planning application.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  provider: ^6.1.1
  http: ^1.2.0
  intl: ^0.19.0
  camera: ^0.10.5+9
  file_picker: ^8.0.0
  flutter_markdown: ^0.6.18
  webview_flutter: ^4.7.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
